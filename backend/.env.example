# ============================================================
# VehicleSouq Backend - Environment Variables
# Copy this file to .env and fill in your actual values
# ============================================================

# MongoDB Atlas connection string (already set up on free tier)
MONGODB_URI=mongodb+srv://<user>:<password>@cluster0.qp8ol.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0

# JWT secret key (keep this secret!)
JWT_SECRET_KEY=5072851946b935d4a9eae4a277d18f71f77781f3a0164fb41b122190e2ff88ca

# CORS - your Vercel frontend URL (add after deploying to Vercel)
# Example: ALLOWED_ORIGINS=https://vehicle-souq.vercel.app,http://localhost:3000
ALLOWED_ORIGINS=http://localhost:3000

# Server config (defaults work fine, change only if needed)
HOST=0.0.0.0
PORT=8000
